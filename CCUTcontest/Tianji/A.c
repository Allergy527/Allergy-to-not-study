#include<stdio.h>

int main(){ 
    long long int n,m,i,j,sum=0;
    while(scanf("%lld",&n)!=EOF){
        printf("%lld\n",(n*n+n)/2);
        printf("\n");
    }
    //getchar();getchar();
    return 0;
}